---
import MarketingLayout from '@/layouts/MarketingLayout.astro';
import DemoRequestForm from '@forms/DemoRequestForm.astro';
import { Icon } from 'astro-icon/components';
import { siteConfig } from '@/config';

const benefits = [
  {
    icon: 'lucide:presentation',
    title: 'Demo personalizada',
    description: 'Descubre cómo afinate puede resolver tus retos de procesos financieros y contabilidad.',
  },
  {
    icon: 'lucide:message-circle',
    title: 'Orientación experta',
    description: 'Obtén respuestas a tus preguntas técnicas de nuestro equipo de soluciones.',
  },
  {
    icon: 'lucide:calculator',
    title: 'Precios personalizados',
    description: 'Recibe una cotización ajustada al tamaño de tu equipo y necesidades.',
  },
  {
    icon: 'lucide:rocket',
    title: 'Soporte de migración',
    description: 'Conoce nuestro programa de asistencia para adoptar la plataforma.',
  },
];

const faqs = [
  {
    question: '¿Cuánto dura la demo?',
    answer: 'Las demos suelen durar 30-45 minutos, incluyendo tiempo para preguntas.',
  },
  {
    question: '¿Quién debería asistir?',
    answer:
      'Recomendamos incluir a los responsables técnicos y líderes de equipo que usarán la plataforma.',
  },
  {
    question: '¿Hay algún compromiso?',
    answer: 'No se requiere ningún compromiso. La demo es completamente gratuita y sin condiciones.',
  },
  {
    question: '¿Puedo obtener una prueba después de la demo?',
    answer: '¡Sí! Ofrecemos pruebas extendidas para equipos que evalúan afinate para uso empresarial.',
  },
];
---

<MarketingLayout
  title={`Solicitar demo - ${siteConfig.name}`}
  description="Reserva una demo personalizada con nuestro equipo y descubre cómo afinate puede transformar tu gestión financiera."
>
  <section class="py-section bg-background">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-16">
        {/* Left: Content */}
        <div>
          <span
            class="inline-flex items-center px-3 py-1 rounded-full text-xs font-medium bg-primary/10 text-primary mb-4"
          >
            Reservar demo
          </span>
          <h1 class="text-4xl sm:text-5xl font-bold text-text mb-6">Descubre afinate en acción</h1>
          <p class="text-xl text-text-muted mb-8 leading-relaxed">
            Obtén un recorrido personalizado de nuestra plataforma y descubre cómo afinate puede ayudar a tu equipo a automatizar la contabilidad, organizar la información financiera y tomar decisiones con datos al día.
          </p>

          {/* Benefits */}
          <div class="grid grid-cols-1 sm:grid-cols-2 gap-6 mb-12">
            {
              benefits.map((benefit) => (
                <div class="flex gap-4">
                  <div class="shrink-0 w-10 h-10 rounded-lg bg-primary/10 flex items-center justify-center">
                    <Icon name={benefit.icon} class="w-5 h-5 text-primary" />
                  </div>
                  <div>
                    <h3 class="font-semibold text-text mb-1">{benefit.title}</h3>
                    <p class="text-sm text-text-muted">{benefit.description}</p>
                  </div>
                </div>
              ))
            }
          </div>

          {/* Social proof */}
          <div class="flex items-center gap-4 p-4 rounded-xl bg-surface border border-border">
            <div class="flex -space-x-2">
              {
                [1, 2, 3, 4].map((i) => (
                  <div class="w-10 h-10 rounded-full bg-primary/20 border-2 border-background flex items-center justify-center text-xs font-medium text-primary">
                    {String.fromCharCode(64 + i)}
                  </div>
                ))
              }
            </div>
            <div>
              <p class="text-sm font-medium text-text">Únete a más de 10.000 equipos</p>
              <p class="text-xs text-text-muted">que confían en afinate para sus procesos financieros</p>
            </div>
          </div>
        </div>

        {/* Right: Form */}
        <div class="lg:pl-8">
          <div class="bg-surface border border-border rounded-2xl p-8">
            <h2 class="text-2xl font-bold text-text mb-6">Programa tu demo</h2>
            <DemoRequestForm />
          </div>
        </div>
      </div>
    </div>
  </section>

  {/* FAQ Section */}
  <section class="py-section bg-surface">
    <div class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8">
      <h2 class="text-2xl font-bold text-text text-center mb-8">Preguntas frecuentes</h2>
      <div class="space-y-4">
        {
          faqs.map((faq) => (
            <div class="p-6 rounded-xl bg-background border border-border">
              <h3 class="font-semibold text-text mb-2">{faq.question}</h3>
              <p class="text-text-muted">{faq.answer}</p>
            </div>
          ))
        }
      </div>
    </div>
  </section>
</MarketingLayout>
