---
import MarketingLayout from '@/layouts/MarketingLayout.astro';
import Hero from '@sections/marketing/Hero.astro';
import HowItWorks from '@sections/marketing/HowItWorks.astro';
import Newsletter from '@sections/marketing/Newsletter.astro';
import { siteConfig } from '@/config';
import ParticlesHero from '@/components/ParticlesHero.astro';
---

<MarketingLayout
  title={`${siteConfig.name} - ${siteConfig.description}`}
  description={siteConfig.description}
>
  <section class="relative w-full overflow-hidden bg-background">
    <ParticlesHero />
    
    <Hero
      title="Procesos Autónomos para tu Empresa"
      subtitle="Trabaja con la confianza de tener un equipo financiero de IA al alcance de una suscripción."
      primaryCTA={{ label: 'Comienza gratis', href: '/register' }}
      textColor="auto"
    />
  </section>

  <section
    aria-label="En 3 pasos sencillos"
    class="py-section pt-0 bg-background"
  >
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="max-w-3xl mx-auto text-center mb-10">
        <h2 class="text-3xl sm:text-4xl font-bold text-text">En 3 pasos sencillos</h2>
        <p class="mt-4 text-lg text-text-muted">
          Desde el registro hasta ver los resultados de tu negocio, todo está pensado para que tengas una experiencia autónoma.
        </p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
        <article class="flex flex-col rounded-2xl border border-border bg-surface shadow-sm overflow-hidden">
          <h3 class="text-xl font-semibold text-text text-center py-4 bg-surface">1. Registra tu información</h3>
          <img src="/registroafinate.jpeg" alt="Registro" loading="lazy" class="w-full h-auto object-contain bg-muted" />
          <div class="flex-1 p-6 flex flex-col">
            <p class="text-sm text-text-muted leading-relaxed">
              Completa tu nombre, correo electrónico y elige una contraseña. Al registrarte se crea tu cuenta y se te activa el acceso al <strong class="text-text">tablero inteligente</strong>.
            </p>
          </div>
        </article>

        <article class="flex flex-col rounded-2xl border border-border bg-surface shadow-sm overflow-hidden">
          <h3 class="text-xl font-semibold text-text text-center py-4 bg-surface">2. Envía tus documentos</h3>
          <img src="/whatsappafinate.jpeg" alt="WhatsApp" loading="lazy" class="w-full h-auto object-contain bg-muted" />
          <div class="flex-1 p-6 flex flex-col">
            <p class="text-sm text-text-muted leading-relaxed">
            Envía tus facturas, recibos y extractos bancarios al número de <strong class="text-text"> Whatsapp +57 321 390 2280</strong> o súbelos directamente en el tablero inteligente.
            </p>
          </div>
        </article>

        <article class="flex flex-col rounded-2xl border border-border bg-surface shadow-sm overflow-hidden">
          <h3 class="text-xl font-semibold text-text text-center py-4 bg-surface">3. Revisa tu tablero</h3>
          <img src="/dashboardafinate.jpeg" alt="Dashboard" loading="lazy" class="w-full h-auto object-contain bg-muted" />
          <div class="flex-1 p-6 flex flex-col">
            <p class="text-sm text-text-muted leading-relaxed">
              Nuestros agentes de IA se encargan de <strong class="text-text">recibir, contabilizar, conciliar, consolidar, proyectar y analizar</strong> tu información, para que siempre tomes las mejores decisiones.
            </p>
          </div>
        </article>
      </div>
    </div>
  </section>

  <section id="descarga-app" aria-label="Descarga app y novedades">
    <Newsletter />
  </section>
</MarketingLayout>